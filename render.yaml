services:
  - type: web
    name: realestate-mappro
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python backend/simple_backend.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: ANTHROPIC_API_KEY
        sync: false
